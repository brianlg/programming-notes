#include <stdio.h>

int main()
{
    /* for ( int x=0; x < 500; x=x+5 )
    {
        printf("the value of x is %d\n", x);
    } */
    
    for ( int i = 1000; i > -1; i=i-13 )
    {
        printf("int i is: %d", i);
    }
    
    
    return 0;
}
